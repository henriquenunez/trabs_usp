#include <stdio.h>

int greater_than_children(int vec[], int index)
{
	int aux;
	//on the heap vector, the children for a given node are 2i + 1 and 2i + 2
	if(vec[index] < vec[2*index+1] || vec[index] < vec[2*index+2])
	{
		//swap with greater
		if (vec[2*index+1] > vec[2*index+1])
		{
			
		} 
		else 
		{


		}
	}

}


void rearrange()
{
	


}